# Copy this file to .env.local and fill in your pCloud app key (client id).
# Register an app at https://docs.pcloud.com/my_apps/ to get one.
# Without this, the app runs in local-only mode (songs in this browser only).
VITE_PCLOUD_CLIENT_ID=
